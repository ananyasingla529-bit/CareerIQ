# CareerIQ — Daily Build Prompt (30-Day Growth Plan)
**Reusable prompt — only the day number changes. Paste this at the start of each day's session.**

---

Today is Day [X] of my 30-Day Growth Plan for CareerIQ, continuing from my 10-day capstone build.

Use `30-day-growth-plan.md` as the source of truth for what to build today — find Day [X]'s milestone and treat it as the only scope for this session. Do not redesign the project, skip ahead, or expand beyond today's listed milestone.

If you need context on the existing codebase, architecture, or past decisions, ask me to upload the relevant files from `docs/` (PRD, ARCHITECTURE.md, SCHEMA.md, API.md, PROJECT-STRUCTURE.md) or `PROGRESS.md` — don't guess or assume.

Standing rules, same as the original capstone:
- Use only free tools, APIs, and services unless I explicitly approve a paid one.
- Assume I have the technical experience I've demonstrated so far, but still explain manual steps (installs, deployments, dashboard configuration) exactly, with real button/menu names, and wait for my confirmation before continuing.
- Prioritize implementation over explanation — generate complete, production-ready code and files, not snippets or placeholders.
- If a change conflicts with the locked architecture decisions in `ARCHITECTURE.md` or the JSON schema in `SCHEMA.md`, flag it and ask before proceeding, don't silently redesign.
- If today's milestone reveals a bigger idea than fits today's scope, log it as a note rather than expanding today's work.
- Verify what we build today actually works — test it, don't just generate it — before considering the day done.

At the end of today's session:
- Confirm everything built today works correctly and hasn't broken anything from previous days.
- Update `PROGRESS.md` with today's entry, following the same format as the existing days.
- Help me commit and push today's work to GitHub with a clear, specific commit message.
- Give me a concise summary: what was completed today, and what tomorrow's milestone (Day [X+1]) will be.

Let's begin Day [X].
